other_ltd_pilot_metrics as (
    SELECT
        ISBN13,
        release_date,
        STRUCT(SUM(value) as value) as metrics,
    FROM
        `{{ other_ltd_pilot_table_id }}`
    GROUP BY
        ISBN13,
        release_date
)
