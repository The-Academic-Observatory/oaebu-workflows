scholarspace_views_pilot_metrics as (
    SELECT
        ISBN13,
        release_date,
        value,
    FROM
        `{{ scholarspace_views_pilot_table_id }}`
    GROUP BY
        ISBN13,
        release_date
)
