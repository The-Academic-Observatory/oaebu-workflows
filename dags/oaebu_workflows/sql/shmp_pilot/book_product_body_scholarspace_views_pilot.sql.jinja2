scholarspace_views_metrics as (
    SELECT
        ISBN13,
        release_date,
        value,
    FROM
        `{{ scholarspace_views_table_id }}`
    GROUP BY
        ISBN13,
        release_date
),
