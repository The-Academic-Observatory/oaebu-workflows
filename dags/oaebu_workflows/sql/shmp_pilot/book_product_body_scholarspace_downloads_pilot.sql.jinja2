scholarspace_downloads_metrics as (
    SELECT
        ISBN13,
        release_date,
        value,
    FROM
        `{{ scholarspace_downloads_table_id }}`
    GROUP BY
        ISBN13,
        release_date
),
